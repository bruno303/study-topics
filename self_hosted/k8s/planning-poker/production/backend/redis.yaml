apiVersion: apps/v1
kind: Deployment
metadata:
  name: planning-poker-redis
  namespace: production
  labels:
    app: planning-poker-redis
    env: production
spec:
  replicas: 1
  selector:
    matchLabels:
      app: planning-poker-redis
      env: production
  template:
    metadata:
      labels:
        app: planning-poker-redis
        env: production
    spec:
      containers:
      - name: planning-poker-redis
        image: redis:7-alpine
        ports:
        - containerPort: 6379
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "256Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: planning-poker-redis
  namespace: production
  labels:
    app: planning-poker-redis
    env: production
spec:
  type: ClusterIP
  selector:
    app: planning-poker-redis
    env: production
  ports:
    - port: 6379
      targetPort: 6379
