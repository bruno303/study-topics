apiVersion: v1
kind: Service
metadata:
  name: planning-poker-backend-svc
  namespace: production
spec:
  type: ClusterIP
  selector:
    app: planning-poker-backend
    env: production
  ports:
    - port: 8080
      targetPort: 8080
