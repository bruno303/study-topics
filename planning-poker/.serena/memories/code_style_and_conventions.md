Follow existing layered boundaries: keep domain logic in internal/domain(+entity), use cases in internal/application/planningpoker/usecase, adapters in internal/infra. Go naming: PascalCase exported, camelCase local helpers; explicit DTO names; context passed through; errors wrapped with fmt.Errorf(...: %w). Frontend conventions: strict TypeScript (avoid any), use @/ imports from src, PascalCase components, camelCase hooks/functions/vars, keep hooks/components focused, and handle room/socket/network errors explicitly. Prefer minimal targeted changes over broad refactors.