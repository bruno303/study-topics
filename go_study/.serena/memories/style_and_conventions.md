Follow AGENTS.md rules: preserve clean layering (application independent from infra), keep minimal focused diffs, use gofmt formatting, standard Go import grouping, descriptive naming (PascalCase exported/camelCase unexported), context.Context first parameter, early error returns with wrapping when useful, use existing logging/tracing abstractions (log.Log, trace.Trace/InjectError/InjectAttributes), avoid secrets in logs, deterministic tests (often table-driven), regenerate mocks when interfaces change (go:generate/mockgen).